root = "."
tmp_dir = "tmp"

[build]
  bin = "./tmp/api"
  cmd = "go build -o ./tmp/api ./cmd/api"
  delay = 1000
  exclude_dir = ["tmp", "tests", "vendor"]
  exclude_file = ["*_test.go"]
  exclude_regex = ["_test.go"]
  include_ext = ["go"]
  kill_delay = "0s"

[screen]
  clear_on_rebuild = true
